# .readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

# Build documentation with Sphinx
sphinx:
  configuration: docs/conf.py

# Optional formats
formats:
  - pdf
  - epub

# Python requirements
python:
  install:
    - requirements: docs/requirements.txt
